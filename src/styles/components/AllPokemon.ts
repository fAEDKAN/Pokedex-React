import styled from "styled-components";

export const AllPokemonCards = styled.div`
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 1.25em;
  margin: 7.5em 15em 0em;
  position: relative;
`;
